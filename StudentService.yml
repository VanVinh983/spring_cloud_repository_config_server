eureka:
  client:
    register-with-eureka:
      value: true
    fetch-registry:
      value: true
    service-url:
      defaultZone: http://localhost:8000/eureka/
    instance:
      hostname: localhost
